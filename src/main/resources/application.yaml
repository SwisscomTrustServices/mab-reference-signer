qtsp:
  oidc:
    discovery-path: "${DISCOVERY_PATH:https://example.invalid/.well-known/openid-configuration}"
  client:
    client-id: "${CLIENT_ID:00000000-0000-0000-0000-000000000000}"
    client-secret: "${CLIENT_SECRET:00000000-0000-0000-0000-000000000000}"
    redirect-uri: "${REDIRECT_URI:https://example.invalid/callback}"
  mtls:
    base-url: "${MTLS_BASE_URL:https://example.invalid}"
    client-cert: "${MTLS_CLIENT_CERT:classpath:demo-client.pem}"
    client-key: "${MTLS_CLIENT_KEY:classpath:demo-client.key}"

server:
  port: 8081

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.sts.demo.signer: INFO
    reactor.netty: WARN
    io.netty: WARN
    io.netty.handler.ssl.BouncyCastlePemReader: ERROR

spring:
  config:
    import: "optional:file:.env[.properties]"
  profiles:
    active: "${SPRING_PROFILES_ACTIVE:dev}"
